{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$defs": {
    "Diff": {
      "properties": {
        "difftool": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "engine": {
          "type": "string",
          "enum": [
            "builtin",
            "filter"
          ]
        },
        "filter": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "layout": {
          "type": "string",
          "enum": [
            "unified",
            "side-by-side"
          ]
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "Duration": {
      "type": "string",
      "pattern": "^([0-9]+(\\.[0-9]+)?(ns|us|µs|ms|s|m|h))+$",
      "description": "Go duration such as 500ms, 10s, 1h30m, or 1.5s"
    },
    "Interactive": {
      "properties": {
        "reader": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "cacheMaxEntries": {
          "type": "integer",
          "minimum": 0
        },
        "cacheTtl": {
          "$ref": "#/$defs/Duration"
        },
        "dock": {
          "type": "string",
          "enum": [
            "left",
            "bottom"
          ]
        },
        "historyLimit": {
          "type": "integer",
          "minimum": 1
        },
        "navigator": {
          "type": "string",
          "enum": [
            "tree",
            "list"
          ]
        },
        "noteInput": {
          "type": "string",
          "enum": [
            "popup",
            "editor"
          ]
        },
        "progress": {
          "type": "boolean"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "progress"
      ]
    },
    "Notes": {
      "properties": {
        "editor": {
          "items": {
            "type": "string"
          },
          "type": "array"
        },
        "generator": {
          "type": "string"
        },
        "generatorTimeout": {
          "$ref": "#/$defs/Duration"
        },
        "refreshInterval": {
          "$ref": "#/$defs/Duration"
        },
        "store": {
          "type": "string"
        }
      },
      "additionalProperties": false,
      "type": "object"
    },
    "Providers": {
      "properties": {
        "cacheMaxEntries": {
          "type": "integer",
          "minimum": 0
        },
        "cacheTtl": {
          "$ref": "#/$defs/Duration"
        },
        "directory": {
          "type": "string"
        },
        "group": {
          "type": "string"
        },
        "timeout": {
          "$ref": "#/$defs/Duration"
        }
      },
      "additionalProperties": false,
      "type": "object"
    }
  },
  "properties": {
    "color": {
      "type": "string",
      "enum": [
        "auto",
        "always",
        "never"
      ]
    },
    "diff": {
      "$ref": "#/$defs/Diff"
    },
    "interactive": {
      "$ref": "#/$defs/Interactive"
    },
    "notes": {
      "$ref": "#/$defs/Notes"
    },
    "providers": {
      "$ref": "#/$defs/Providers"
    }
  },
  "additionalProperties": false,
  "type": "object",
  "title": "Changes configuration"
}
